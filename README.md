# stm32-register-level-lcd-driver
This project demonstrates how to interface a 16x2 HD44780 LCD with the STM32F407 Discovery Board using register-level programming without HAL or STM32Cube libraries.
The LCD is configured in 4-bit mode, and all GPIO configuration is performed by directly accessing STM32 peripheral registers.
